[project]
name = "youtube-ripper"
version = "0.1.0"
description = "Extract audio from YouTube, transcribe with Whisper, generate PDFs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faster-whisper>=1.2.1",
    "flask>=3.1.2",
    "weasyprint>=68.1",
    "yt-dlp>=2026.2.4",
]
